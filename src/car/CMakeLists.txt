# Define the CarLibrary target
add_library(CarLibrary car.cpp car.h)

# Set the include directories for this target (if needed)
# target_include_directories(CarLibrary PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
