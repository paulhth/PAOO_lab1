# Define the MilitaryCarLibrary target
add_library(VehicleLibrary vehicle.cpp vehicle.h)

# Set the include directories for this target (if needed)
# target_include_directories(MilitaryCarLibrary PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})
